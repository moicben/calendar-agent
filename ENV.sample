# Copiez ce fichier vers .env et complétez les valeurs

# Clé OpenAI (requise par browser-use/ChatOpenAI)
OPENAI_API_KEY=

# Chemin Chrome (macOS par défaut). Sur Linux dans la VM Orbstack, utilisez
# /usr/bin/google-chrome ou /usr/bin/chromium-browser selon votre installation
CHROME_PATH=/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome

# Dossier de profil chrome persistant
BROWSERUSE_USER_DATA_DIR=./browseruse-profile

# Exécution headless par défaut (true conseillé en VM)
BROWSERUSE_HEADLESS=true

# Active DevTools (désactivé si headless=true par défaut)
BROWSERUSE_DEVTOOLS=false

# Modèle LLM
BROWSERUSE_MODEL=gpt-4o-mini

# Réseau/serveur
HOST=0.0.0.0
PORT=8080

# Uvicorn auto-reload (false en prod)
UVICORN_RELOAD=false

# Serper.dev API key (pour scraper.py)
SERPER_API_KEY=

